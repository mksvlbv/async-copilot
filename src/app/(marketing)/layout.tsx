/**
 * Marketing surface layout — Landing, future marketing pages.
 * Minimal shell for Unit 1 scaffold; real header/footer arrives in Unit 4.
 */
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
