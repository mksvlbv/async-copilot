/**
 * App surface layout — workspace shell.
 * Minimal wrapper for Unit 1 scaffold; real app-shell with nav arrives in Unit 5.
 */
export default function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
