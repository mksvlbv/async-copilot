; Allow installs despite transitive peer-dep conflicts.
; Required because audit tooling pulls dotenv@16 (stagehand) and dotenv@17
; (axe-core/cli) as conflicting peer deps. Both work fine at runtime — the
; audit scripts are dev-only and never run in the production bundle.
legacy-peer-deps=true
