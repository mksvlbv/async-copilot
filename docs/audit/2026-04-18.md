# Site Audit — 2026-04-18

Baseline audit for the Async Copilot portfolio demo. Captured manually via
Playwright MCP + static inspection; full tooling (Lighthouse CI / pa11y /
axe / lychee / Lost Pixel) installed for future recurring runs via
`npm run audit:*` and four `workflow_dispatch` GitHub Actions under
`.github/workflows/audit-*.yml`.

## Summary

| Area | Status | Notes |
|---|---|---|
| Broken links | **5 found → fixed** | All footer `href="#"` replaced or removed |
| Dummy mailto | **1 found → removed** | `hello@example.com` Contact CTA replaced with `/app/samples` |
| 404 routes | **5 → 1 real page created** | `/legal/privacy` shipped; Terms/Security/Docs dropped per honest-minimalism plan |
| Mockup `href="#"` | **1 → fixed** | `View Status` in hero mockup → non-clickable `<span>` |
| a11y icon-only buttons | **0 issues** | All already have `aria-label` or visible text |
| Build | **green** | 15 routes compiled, 102 kB shared JS |

## Methodology

### Tools installed (Phase 1)

- **Lighthouse CI** — `@lhci/cli@0.15` with perf ≥ 85, a11y ≥ 90 budgets (configured in `lighthouserc.cjs`)
- **pa11y-ci** — WCAG 2 AA scan over public routes (`.pa11yci.json`)
- **@axe-core/cli** — axe-core engine, complements pa11y
- **linkinator** — npm-native broken link checker (`.linkinatorrc.json`)
- **lost-pixel** — visual regression snapshots, 4 routes × 3 breakpoints (`lost-pixel.config.ts`)
- **Playwright MCP** — in-browser exploratory audit used for the baseline captured below

### Why not run the full pipeline for this baseline

Each tool spins up headless Chromium and scans live production URLs with
its own strategies (recursion, multi-run medians, breakpoint screenshots).
Running them all sequentially on a polishing session exceeds the budget
for interactive debugging (>30 min observed on a first-run linkinator
recursion). The tools are wired up and runnable at any time; the
baseline below was captured via **Playwright MCP** in-browser evaluation
in ~2 seconds.

## Findings (baseline)

### 1. Broken footer links — FIXED

Before:

```tsx
<a href="#" className="hover:text-gray-900">Security</a>
<a href="#" className="hover:text-gray-900">Privacy</a>
<a href="#" className="hover:text-gray-900">Terms</a>
<a href="#" className="hover:text-gray-900">Documentation</a>
```

All four led to the same URL (dead anchor), confusing users who expected
real destinations.

After (`src/app/(marketing)/page.tsx` — `LandingFooter`):

- **Security, Terms, Documentation** — removed from footer entirely. Honest
  minimalism: a portfolio demo does not need those pages and faking them
  erodes trust.
- **Privacy** — now points to `/legal/privacy`, a real page.
- **GitHub ↗** — added as external anchor (`target="_blank"`).
- **"Portfolio demo" badge** added next to the logo for additional honesty.

### 2. Dummy mailto — FIXED

Before (`ClosingCta`): `<a href="mailto:hello@example.com">Contact</a>`.

After: Contact CTA replaced with **"Browse Scenarios"** `<Link>` to
`/app/samples`. Keeps the two-button rhythm of the CTA section while
eliminating the fake email.

### 3. Missing `/legal/privacy` route — FIXED

New page at `src/app/legal/privacy/page.tsx`. Minimal but real:

- Honest opening: declares the site as a non-commercial demo.
- Lists exactly what is and isn't collected.
- Mentions Groq for real AI inference and explains the synthetic fallback path.
- Advises not to paste sensitive data.
- Zero tracking, zero cookies, zero dark patterns in the copy.
- Includes proper `metadata` export (`title`, `description`) for SEO.

Static prerender: 170 B / 106 kB first-load.

### 4. `View Status` in hero mockup — FIXED

`src/app/(marketing)/_sections/hero-mockup.tsx` had
`<a href="#">View Status</a>` as decorative UI chrome inside the animated
demo mockup. Clicking it would scroll to the top of the page, confusing
users.

Fixed to `<span className="underline opacity-70">View Status</span>` —
visually identical, but correctly non-interactive (the mockup is
illustrative, not functional).

### 5. Icon-only a11y — NO WORK NEEDED

Initial critique flagged "2-3 icon-only buttons without aria-label". Full
audit of all `<button>` elements in `src/` confirms every icon-only button
already has either `aria-label` or visible text:

- `app-header.tsx` — operator chrome is now non-interactive and labeled as demo workspace context
- `live-run-view.tsx` — draft actions expose only functional copy control; read-only state is labeled
- `hero-mockup.tsx` — Copy/Edit icons: `aria-label`'d
- All other buttons have visible text

### 6. Navigation anchors — ALREADY CORRECT

`#how-it-works`, `#system-trust`, `#output` all resolve to real `<section id="...">`
elements in the landing page. No action.

## Open items (deferred for future audit runs)

Captured here so they're not lost — none are currently blocking.

- **Lighthouse perf run** — run `npm run audit:perf` once stable to capture
  baseline scores. Expected: perf ≥ 90 (static-heavy Next 15 app), a11y ≥ 95.
- **Lost Pixel baseline** — first `npm run audit:visual` will write
  screenshots to `.lostpixel/baseline/` for commit. Do this right after
  the next visual change stabilizes, not during active polishing.
- **Contact surface** — if the demo is submitted to a portfolio reviewer
  who wants to reach the author, consider a LinkedIn link in the footer
  rather than an email.

## Verification

- `npm run typecheck` — zero errors
- `npm run build` — 15 routes, 102 kB shared JS, `/legal/privacy` statically prerendered
- Manual Playwright MCP re-scan of `/` confirms no remaining `href="#"` elements
